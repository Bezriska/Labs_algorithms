# include <stdio.h>
# include <string.h>
# include "tree.h"
# include "read.h"


int main() {
    
    Node* root = read(1);
    if (root == NULL) {
        printf("Дерево пустое или файл не найден\n");
        return 1;
    }

    




}